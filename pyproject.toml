[project]
name = "vanilla-caves"
version = "0.1.0"
description = "A datapack that removes noise caves and only generates carver caves."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "beet>=0.114.1",
]
