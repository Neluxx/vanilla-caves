[project]
name = "vanilla-caves"
version = "0.2.1"
description = "A datapack that removes noise caves and only generates carver caves."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "beet>=0.115.0",
]
